# Secrets

host all secrets

vault, vaultwarden and keycloak for cloud infra

1. Create a docker host instance in cloud
2. Deploy Vault, Vaultwarden and Keycloak by docker compose `./compose`
3. Save all the secrets in vault `./valut`
