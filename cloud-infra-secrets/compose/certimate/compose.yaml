services:
  certimate:
    container_name: certimate
    image: certimate/certimate:latest
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      - certimate_data:/app/pb_data
    restart: unless-stopped

volumes:
  certimate_data:
